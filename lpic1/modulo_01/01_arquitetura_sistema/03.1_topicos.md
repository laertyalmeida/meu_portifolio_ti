# Kernel, initramfs e systemd

1. **Kernel**

   * Função
   * Comunicação com o hardware
   * Gerenciamento de recursos

2. **Processo de inicialização**

   * BIOS / UEFI
   * Bootloader
   * Kernel
   * initramfs
   * systemd

3. **initramfs**

   * Initial RAM Filesystem
   * Função
   * Drivers e módulos
   * Acesso ao sistema de arquivos principal

4. **initrd × initramfs**

   * initrd
   * initramfs
   * Diferenças

5. **systemd**

   * Sistema de inicialização
   * Gerenciamento de serviços

6. **PID 1**

   * Primeiro processo
   * systemd como PID 1

7. **Serviços**

   * `systemctl`
   * `start`
   * `stop`
   * `restart`
   * `status`
   * `enable`

8. **Units**

   * `.service`
   * `.socket`
   * `.mount`
   * `.target`
   * `.timer`
   * `.device`

9. **Targets**

   * `multi-user.target`
   * `graphical.target`

10. **Dependências**

    * Ordem de inicialização
    * Relação entre serviços

11. **Fluxo completo de inicialização**

    * Firmware
    * Bootloader
    * Kernel
    * initramfs
    * Sistema de arquivos
    * systemd
    * Serviços
    * Sistema Linux

